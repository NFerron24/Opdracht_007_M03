const calculateDogAge = function (Age) {
  var PuppyAge = 7 * Age;
  console.log(
    "Your doggie's age is" + " " + PuppyAge + " " + "years old in dog years"
  );
};

calculateDogAge(9);
calculateDogAge(100);
